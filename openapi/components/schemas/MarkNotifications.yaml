type: object
required:
  - status
  - notificationIds
properties:
  status:
    type: string
    enum:
      - read
      - unread
  notificationIds:
    type: array
    items:
      $ref: ../schemas/NotificationId.yaml
